- hosts: all
  become: true

  environment:
    PATH: "{{ (node_bin_dir ~ ':') if node_bin_dir | default('') else '' }}{{ ansible_env.PATH }}"

  vars:
    root_path: /opt/deployment/frontend/saathi
    project_path: "{{root_path}}/saathi-frontend"
    vault_token_path: /opt/deployment/.token
    repo_url: https://github.com/ELEVATE-Project/saathi-frontend
    vault_env_path: saathi_env
    vault_pm2_config_path: saathi_pm2Config

  tasks:
    - name: Slurp host file
      slurp:
        src: "{{vault_token_path}}"
      register: slurpfile

    - name: Resolve nvm-managed Node bin directory
      shell: |
        set -euo pipefail
        export NVM_DIR=/root/.nvm
        . "$NVM_DIR/nvm.sh"
        resolved="$(dirname "$(nvm which default)")"
        case "$resolved" in
          /*) ;;
          *) echo "Resolved node bin dir '$resolved' is not an absolute path - nvm resolution failed" >&2; exit 1 ;;
        esac
        echo "$resolved"
      args:
        executable: /bin/bash
      register: nvm_node_bin
      changed_when: false

    - name: Set Node bin directory fact
      set_fact:
        node_bin_dir: "{{ nvm_node_bin.stdout }}"

    - name: Debug resolved node bin directory
      debug:
        msg: "node_bin_dir={{ node_bin_dir }}"

    - name: Ensure project directory exists
      file:
        path: "{{ project_path }}"
        state: directory
        mode: '0755'

    - name: Fetch the latest code
      git:
        repo: "{{ repo_url }}"
        dest: "{{ project_path }}"
        version: "{{ gitBranch }}"
        force: yes
    
    - name: set permission
      shell: chmod 744 {{ project_path }}/src/scripts/json2env.sh  

    - name: Fetch .env from Vault
      shell: >
        curl --insecure --http1.1 --location --request GET '{{ vaultAddress }}{{ vault_env_path }}'
        --header 'X-Vault-Token: {{ slurpfile['content'] | b64decode }}' | jq '.data.data' > '{{ project_path }}/data2.json'
      register: env_response

    - name: Read fetched .env data for debugging
      shell: "cat {{ project_path }}/data2.json"
      register: data2_content

    - name: Fail if .env data from Vault is empty or invalid
      fail:
        msg: "Vault .env fetch returned empty or invalid data - check the debug output above and Vault connectivity/secret path ({{ vault_env_path }})."
      when: (data2_content.stdout | trim == '') or not (data2_content.stdout is search('^\s*\{'))

    - name: generate .env 
      shell: cat {{ project_path }}/data2.json | {{ project_path }}/src/scripts/json2env.sh > {{ project_path }}/.env
      register: envConfig 
    - debug: 
        msg: " cred {{ envConfig }} "    

    - name: Remove the old build
      shell: "rm -rf {{project_path}}/build"

    - name: Remove node_modules folder
      shell: "rm -rf {{ project_path }}/node_modules"

    - name: Clear npm cache
      shell: "{{ node_bin_dir }}/npm cache clean --force"

    - name: Install dependencies
      shell: "cd {{ project_path }} && {{ node_bin_dir }}/npm i --force"

    - name: Set permission for json2env.sh
      shell: "chmod 744 {{ project_path }}/src/scripts/json2env.sh"

    - name: Fetch pm2 config file
      shell: >
        curl --insecure --http1.1 --location --request GET '{{ vaultAddress }}{{ vault_pm2_config_path }}'
        --header 'X-Vault-Token: {{ slurpfile['content'] | b64decode }}' 
        | jq '.data.data' > '{{ project_path }}/pm2.config.json'
      register: pm2_fetch_response

    - name: Read fetched pm2 config for debugging
      shell: "cat {{ project_path }}/pm2.config.json"
      register: pm2_content
    
    - name: Debug Vault pm2 config response (for troubleshooting)
      debug:
        msg: "{{ pm2_content.stdout }}"

    - name: Build saathi app
      shell: "cd {{ project_path }} && npm run build"
      environment:
        NODE_OPTIONS: "--max-old-space-size=4096"

    - name: Start pm2
      shell: "cd {{ project_path }} && pm2 start pm2.config.json"
